version = "1.0.4"
description = "Makes Spotify and Youtube Embeds playable"

aliucord.changelog.set("""
    # 1.0.4
    * Youtube Clips now embed as well
    * Settings added to disable each provider as well as specify an endpoint for Clips support
    # 1.0.3
    * All embedded videos are now playable in-line.
    # 1.0.2
    * Youtube embed now supports timestamps
""".trimIndent())
